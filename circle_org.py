from organism import organism
from bitstring import BitArray
from PIL import Image, ImageDraw

#circle is a class defined by x and y candiates (2 bytes each) radius (1 byte)
#circle colors is 4 bytes(rgb + alpha)
class circle:
    bit_count 
    def __init__(self,cen,col):
       self.center = center
       self.color = col
    

class image_org(person):
    circle_count = 150 
    ref_image = Image.open("some default image")

    oraganism.DNA_length = circle_count * circle.bit_count#overrides this value
   
    def __init__(self, )

    @staticmethod
    def set_ref_image(img_name):
        image_org.ref_image = Image.open(img_name)
